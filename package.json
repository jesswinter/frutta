{
  "name": "frutta",
  "version": "0.2.2-alpha.0",
  "description": "Command-line pomodoro style timer",
  "bin": "./bin/frutta.js",
  "type": "module",
  "keywords": [
    "pomodoro",
    "timer",
    "productivity"
  ],
  "author": "Jess Winter",
  "license": "MIT",
  "scripts": {
    "dev": "tsc --watch --preserveWatchOutput",
    "typecheck": "tsc -P . --noEmit",
    "build": "tsc",
    "install-global": "npm install -g --production"
  },
  "files": [
    "/dist/**/*"
  ],
  "dependencies": {
    "cli-progress": "^3.12.0"
  },
  "devDependencies": {
    "@types/cli-progress": "^3.11.6",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
